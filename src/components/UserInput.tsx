/* interface Props {
  children: string;
} */

const UserInput = (/* {}: Props */) => {
  return (
    <div id="user-input">
      <div className="input-group"></div>
    </div>
  );
};

export default UserInput;
